logging:
  level:
    root: info
    com.spring.boot.c1: debug
    org.mybatis: debug
    org.springframework.jdbc: debug
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{50} - %msg%n"